'use strict';

/**
 * @ngdoc function
 * @name 03NgBootstrapApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the 03NgBootstrapApp
 */
angular.module('03NgBootstrapApp')
  .controller('MainCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });


  // npm i bower grunt-cli yo -g
  // npm i generator-angular -g
  // yo angular > answer the questions
  // npm install
  // bower install
  // grunt serve